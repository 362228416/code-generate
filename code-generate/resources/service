package ${info.targetPackage};

import ${info.packageName}.${info.className};

import java.util.List;
import java.io.Serializable;

public interface ${info.className}${info.suffix} {
	
	void save${info.className}(${info.className} entity);
	
	void delete${info.className}(${info.className} entity);
	
	void delete${info.className}(Serializable id);
	
	void update${info.className}(${info.className} entity);
	
	${info.className} find${info.className}(Serializable id);
	
	List<${info.className}> findAll${info.className}();
	
	List<${info.className}> find${info.className}s(int firstResult, int maxResult);
	
	List<${info.className}> find${info.className}sForPage(int page, int size);
	
	List<${info.className}> find${info.className}sForPage(int page);
	
}