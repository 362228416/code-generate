package ${target};

import ${fullName};

import java.util.List;
import java.io.Serializable;

public interface ${class}${suffix} {
	
	void save${class}(${class} ${simpleName});
	
	void delete${class}(${class} ${simpleName});
	
	void delete${class}(Serializable id);
	
	void update${class}(${class} ${simpleName});
	
	${class} find${class}(Serializable id);
	
	List<${class}> findAll${class}();
	
	List<${class}> find${class}s(int firstResult, int maxResult);
	
	List<${class}> find${class}sForPage(int page, int size);
	
	List<${class}> find${class}sForPage(int page);
	
}